spring:
    application:
        name: Flash_Cash

    datasource:
        url: jdbc:mysql://localhost:3306/flashcash
        username: root
        password: Dev26awatef
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        database-platform: org.hibernate.dialect.MySQLDialect


#pour vider la cache de thymeleaf en temps réel
    thymeleaf:
        cache: false

logging:
    level:
        org:
            springframework:
                security: DEBUG



